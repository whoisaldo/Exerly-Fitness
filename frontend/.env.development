# Fixes webpack-dev-server "allowedHosts[0] should be a non-empty string" when using
# package.json "proxy" with some Node/webpack-dev-server combinations (e.g. Windows + Node 22).
DANGEROUSLY_DISABLE_HOST_CHECK=true
